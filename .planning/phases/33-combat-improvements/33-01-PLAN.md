---
phase: 33-combat-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/reducers/combat.ts
  - spacetimedb/src/helpers/combat.ts
  - spacetimedb/src/data/combat_scaling.ts
  - spacetimedb/src/helpers/combat.test.ts
  - spacetimedb/src/data/combat_scaling.test.ts
  - src/components/NarrativeMessage.vue
autonomous: true
requirements: [COMB-01, COMB-02, COMB-03, COMB-04, COMB-07]

must_haves:
  truths:
    - "DoT ticks emit narrative-style damage messages with effect name ('You suffer X damage from Y')"
    - "HoT ticks emit narrative-style heal messages with effect name ('Y soothes you for X HP')"
    - "Buff/debuff application emits log entry with stat, magnitude, and duration"
    - "Buff/debuff expiration emits enriched log entry with ability name and stat info"
    - "Ability damage is scaled down to roughly double combat duration"
    - "Mana ability costs are increased relative to stamina costs"
    - "Mana abilities enforce a minimum cast time floor at resolution time"
    - "New event kinds 'buff' and 'debuff' render in correct colors on client"
  artifacts:
    - path: "spacetimedb/src/reducers/combat.ts"
      provides: "Narrative-style DoT/HoT tick messages, enriched expiry messages"
      contains: "You suffer"
    - path: "spacetimedb/src/helpers/combat.ts"
      provides: "Buff/debuff application event emission, mana cast time floor"
      contains: "appendPrivateEvent"
    - path: "spacetimedb/src/data/combat_scaling.ts"
      provides: "ABILITY_DAMAGE_SCALER, increased mana cost scaling"
      exports: ["ABILITY_DAMAGE_SCALER", "MANA_COST_MULTIPLIER"]
    - path: "src/components/NarrativeMessage.vue"
      provides: "Color entries for buff and debuff event kinds"
      contains: "buff:"
  key_links:
    - from: "spacetimedb/src/reducers/combat.ts"
      to: "spacetimedb/src/helpers/events.ts"
      via: "appendPrivateEvent with narrative phrasing"
      pattern: "appendPrivateEvent.*suffer|soothes|grants|worn off"
    - from: "spacetimedb/src/helpers/combat.ts"
      to: "spacetimedb/src/data/combat_scaling.ts"
      via: "ABILITY_DAMAGE_SCALER applied in resolveAbility"
      pattern: "ABILITY_DAMAGE_SCALER"
---

<objective>
Enrich combat log messages with narrative-style DoT/HoT tick entries, buff/debuff lifecycle events, and rebalance combat scaling for longer fights.

Purpose: Players currently see generic combat messages and fights end too fast. This plan delivers immersive per-tick feedback and balance tuning via scaling formulas.
Output: Updated combat reducers/helpers with narrative messages, new balance constants, updated tests, client color mapping for new event kinds.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-combat-improvements/33-CONTEXT.md
@.planning/phases/33-combat-improvements/33-RESEARCH.md

@spacetimedb/src/reducers/combat.ts
@spacetimedb/src/helpers/combat.ts
@spacetimedb/src/data/combat_scaling.ts
@spacetimedb/src/helpers/events.ts
@spacetimedb/src/helpers/combat.test.ts
@spacetimedb/src/data/combat_scaling.test.ts
@src/components/NarrativeMessage.vue

<interfaces>
<!-- Key types and contracts the executor needs -->

From spacetimedb/src/helpers/events.ts:
```typescript
export function appendPrivateEvent(ctx: any, characterId: bigint, ownerUserId: any, kind: string, message: string);
export function logPrivateAndGroup(ctx: any, character: any, kind: string, privateMessage: string, groupMessage?: string);
```

From spacetimedb/src/data/combat_scaling.ts:
```typescript
export const GLOBAL_DAMAGE_MULTIPLIER = 85n;
export function getAbilityMultiplier(castSeconds: bigint, cooldownSeconds: bigint): bigint;
// abilityResourceCost is in helpers/combat.ts:
export function abilityResourceCost(level: bigint, power: bigint);
export function staminaResourceCost(power: bigint);
```

From spacetimedb/src/helpers/combat.ts:
```typescript
export function addCharacterEffect(ctx, characterId, effectType, magnitude, roundsRemaining, sourceAbility);
export function addEnemyEffect(ctx, combatId, enemyId, effectType, magnitude, roundsRemaining, sourceAbility, ownerCharacterId?);
export function resolveAbility(ctx, combatId, actor, abilityRow, targetCharacterId, targetPetId?);
```

From src/components/NarrativeMessage.vue:
```typescript
const KIND_COLORS: Record<string, string> = {
  damage: '#ff6b6b',
  heal: '#69db7c',
  // ... existing kinds
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Combat log narrative messages + buff/debuff lifecycle events</name>
  <files>spacetimedb/src/reducers/combat.ts, spacetimedb/src/helpers/combat.ts, spacetimedb/src/helpers/combat.test.ts, src/components/NarrativeMessage.vue</files>
  <behavior>
    - Test: DoT tick on character emits event with kind='damage' and message matching "You suffer {dmg} damage from {sourceAbility}"
    - Test: HoT tick on character emits event with kind='heal' and message matching "{sourceAbility} soothes you for {magnitude} HP"
    - Test: DoT tick on enemy emits event with message matching "{sourceAbility} sears {enemyName} for {dmg}"
    - Test: addCharacterEffect for buff types (damage_up, armor_up, ac_bonus, etc.) emits event with kind='buff' and message containing stat, magnitude, and duration
    - Test: addCharacterEffect for debuff types (dot, armor_down, stun) emits event with kind='debuff' (dot keeps 'damage' for tick, but application uses 'debuff')
    - Test: addEnemyEffect for debuff application emits event to all combat participants
    - Test: Effect expiry message includes ability name and effect type label (not just "{name} has worn off")
  </behavior>
  <action>
    **In `spacetimedb/src/helpers/combat.test.ts`:**
    Add test cases for DoT/HoT tick message format and buff/debuff application events. Use the existing mock DB pattern from Phase 31. Tests should verify event messages match narrative phrasing and use correct event kinds.

    **In `spacetimedb/src/reducers/combat.ts` — `tickEffectsForRound()`:**
    1. Line 2487-2488 (HoT tick): Change kind from 'ability' to 'heal', change message to: `${effect.sourceAbility ?? 'Regeneration'} soothes you for ${effect.magnitude} HP.`
    2. Line 2493-2494 (DoT on character): Change message to: `You suffer ${dmg} damage from ${effect.sourceAbility ?? 'a lingering effect'}.`
    3. Line 2547-2548 (DoT on enemy): Change message to: `${effect.sourceAbility ?? 'A lingering effect'} sears ${enemy.displayName} for ${dmg}.`
    4. Line 2519-2520 (character effect expiry): Enrich to include effect type label: `The effect of ${effect.sourceAbility ?? effect.effectType} (${effectTypeLabel(effect.effectType)}) has worn off.` Add a simple `effectTypeLabel()` helper locally or import from effectTimers pattern.
    5. Line 2586-2587 (enemy effect expiry): Enrich similarly for participants: `${effect.sourceAbility ?? effect.effectType} on ${enemy.displayName} fades.` (this is already decent, keep but ensure consistency).

    **In `spacetimedb/src/helpers/combat.ts` — `addCharacterEffect()`:**
    1. After the insert/update block (line ~208), emit a buff/debuff application event:
       - For buff types ('regen', 'damage_up', 'armor_up', 'ac_bonus', 'damage_shield', 'hp_bonus', 'magic_resist', 'stamina_free'): `appendPrivateEvent(ctx, characterId, character.ownerUserId, 'buff', '${sourceAbility} grants ${effectTypeLabel(effectType)}${magnitude > 0n ? " +" + magnitude : ""} for ${roundsRemaining} rounds.')`
       - For debuff types ('dot', 'armor_down', 'stun'): Use kind='debuff'. Note: DoT/regen already emit immediate tick messages — the application message is separate (announces the effect starting, not the first tick).
       - Look up the character to get ownerUserId: `const character = ctx.db.character.id.find(characterId);`
    2. In `addEnemyEffect()`: After insert/update, emit to all combat participants using a loop over `combat_participant.by_combat.filter(combatId)` — use kind='debuff': `'${sourceAbility} afflicts ${enemyName} with ${effectTypeLabel(effectType)} for ${roundsRemaining} rounds.'`
    3. For the HoT/DoT immediate tick messages already in addCharacterEffect (lines 217, 221): Update to narrative style matching the tick messages above.

    **In `src/components/NarrativeMessage.vue`:**
    Add two new entries to `KIND_COLORS`:
    ```
    buff: '#74c0fc',    // blue per user decision
    debuff: '#ffa94d',  // orange per user decision
    ```

    **Important:** Do NOT change event kinds for existing combat messages that work correctly. Only change the specific messages identified above.
  </action>
  <verify>
    <automated>cd spacetimedb && npx vitest run src/helpers/combat.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>DoT ticks say "You suffer X damage from Y", HoT ticks say "Y soothes you for X HP", buff/debuff application and expiry events include stat/magnitude/duration, client renders buff=blue and debuff=orange</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Combat balance tuning — ability damage scaler, mana costs, cast time floor</name>
  <files>spacetimedb/src/data/combat_scaling.ts, spacetimedb/src/helpers/combat.ts, spacetimedb/src/data/combat_scaling.test.ts</files>
  <behavior>
    - Test: ABILITY_DAMAGE_SCALER exists and is ~50n (50% on 100n scale, halving ability damage)
    - Test: getAbilityMultiplier result is scaled down by ABILITY_DAMAGE_SCALER
    - Test: MANA_COST_MULTIPLIER exists and is > 100n (e.g., 150n = 50% more expensive)
    - Test: abilityResourceCost for mana abilities returns higher cost than base formula
    - Test: MANA_MIN_CAST_SECONDS exists and is > 0n (e.g., 1n or 2n)
    - Test: resolveAbility enforces minimum cast time for mana-type abilities at resolution (not generation)
    - Test: Minimum damage floor of 1n after scaling (no zero damage)
  </behavior>
  <action>
    **In `spacetimedb/src/data/combat_scaling.ts`:**
    1. Add `export const ABILITY_DAMAGE_SCALER = 50n;` — 50% on 100n scale. This single lever halves all ability damage to approximately double combat duration. Comment: "Scales all ability damage. 50n = abilities deal 50% damage. Adjust to tune combat duration."
    2. Add `export const MANA_COST_MULTIPLIER = 150n;` — 150% on 100n scale, making mana abilities 50% more expensive. Comment: "Mana cost multiplier vs base formula. Design: mana abilities are more plentiful but costlier per-cast than stamina."
    3. Add `export const MANA_MIN_CAST_SECONDS = 1n;` — Minimum cast time for mana abilities. Comment: "Floor cast time for mana-type abilities. Enforced at resolution, not generation."

    **In `spacetimedb/src/helpers/combat.ts` — `resolveAbility()`:**
    1. Import `ABILITY_DAMAGE_SCALER`, `MANA_COST_MULTIPLIER`, `MANA_MIN_CAST_SECONDS` from combat_scaling.
    2. In the damage calculation section of resolveAbility, after computing `scaledPower`, apply: `scaledPower = (scaledPower * ABILITY_DAMAGE_SCALER) / 100n; if (scaledPower < 1n) scaledPower = 1n;`
    3. In the resource cost section, if the ability uses mana (check `ability.resourceType === 'mana'` or detect from ability kind — look at how the code currently distinguishes mana vs stamina), multiply the cost: `cost = (cost * MANA_COST_MULTIPLIER) / 100n;`
    4. For cast time: Before applying the ability, check if it's a mana ability and enforce minimum: `if (isManaAbility && castSeconds < MANA_MIN_CAST_SECONDS) castSeconds = MANA_MIN_CAST_SECONDS;` — This must happen at resolution time so existing abilities with castSeconds=0 are caught. Determine `isManaAbility` from the ability's resource type or kind.

    **In `spacetimedb/src/data/combat_scaling.test.ts`:**
    Add tests verifying the new constants exist with expected ranges, that ABILITY_DAMAGE_SCALER reduces damage output, and that minimum floors prevent zero values.

    **In `spacetimedb/src/helpers/combat.ts` — `abilityResourceCost()`:**
    Add an optional parameter for resource type. When resourceType is 'mana', apply MANA_COST_MULTIPLIER. Keep backward compatible — existing calls without the parameter work unchanged.

    **Critical:** Do NOT modify auto-attack damage (GLOBAL_DAMAGE_MULTIPLIER stays at 85n). Do NOT modify healing formulas. Only ability damage and mana costs change.
  </action>
  <verify>
    <automated>cd spacetimedb && npx vitest run src/data/combat_scaling.test.ts src/helpers/combat.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>ABILITY_DAMAGE_SCALER halves ability damage, MANA_COST_MULTIPLIER increases mana costs by 50%, mana abilities enforce minimum 1s cast time, no zero-damage edge cases, all tests pass</done>
</task>

</tasks>

<verification>
Run full test suite to ensure no regressions:
```bash
cd spacetimedb && npm test
```
Verify NarrativeMessage.vue has 'buff' and 'debuff' in KIND_COLORS.
</verification>

<success_criteria>
- All DoT/HoT tick messages use narrative phrasing with effect name
- Buff/debuff application and expiry events appear in combat log with correct color-coded kinds
- Ability damage is roughly halved via ABILITY_DAMAGE_SCALER
- Mana costs increased via MANA_COST_MULTIPLIER
- Mana abilities enforce minimum cast time at resolution
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/33-combat-improvements/33-01-SUMMARY.md`
</output>
