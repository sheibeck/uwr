---
phase: 33-combat-improvements
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.vue
autonomous: true
requirements: [COMB-06]
gap_closure: true

must_haves:
  truths:
    - "Clicking an enemy name in narrative text during combat initiates a pull if that enemy is not already in the fight"
    - "Clicking an enemy name already in combat sets it as target (existing behavior preserved)"
  artifacts:
    - path: "src/App.vue"
      provides: "Mid-combat enemy click routes to pull for available enemies"
      contains: "availableEnemies"
  key_links:
    - from: "src/App.vue"
      to: "startPull"
      via: "clickNpcKeyword checks availableEnemies during combat"
      pattern: "availableEnemies.*startPull"
---

<objective>
Allow mid-combat pulling by clicking enemy names in narrative text. Currently clicking an enemy name during combat only targets enemies already in the fight -- it never checks if the clicked name is an available (not-yet-pulled) enemy spawn.

Purpose: Server-side mid-combat pull logic exists (from 33-02) but the client blocks it. The clickNpcKeyword handler in App.vue has a !isInCombat guard that prevents enemy-click-to-pull during combat. UAT gap 5 reports: "Clicking on another enemy in the narrative just tries to target selected enemy instead of allowing me to pull it."
Output: Updated clickNpcKeyword handler that checks availableEnemies for pull targets during combat.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-combat-improvements/33-UAT.md
@.planning/phases/33-combat-improvements/33-02-SUMMARY.md

@src/App.vue

<interfaces>
<!-- Key data structures and functions available in App.vue -->

From useCombat composable (already destructured in App.vue):
- availableEnemies: ComputedRef<EnemySummary[]> -- enemies at current location with state 'available' or 'pulling' (NOT in combat)
- combatEnemiesList: ComputedRef<CombatEnemy[]> -- enemies currently in active combat
- startPull(spawnId: bigint, pullType: string): void -- initiates a pull
- isInCombat: ComputedRef<boolean> -- whether character is in active combat

App.vue clickNpcKeyword handler (lines 1360-1420):
- Lines 1365-1379: 'careful pull' and 'charge in' keywords already work with pendingPullTargetId
- Lines 1383-1392: Enemy name click OUTSIDE combat -> sets pendingPullTargetId and calls onNarrativeSubmit('attack')
- Lines 1395-1420: During combat, enemy names only match combatEnemiesList for targeting
- Line 1411-1419: In-combat enemy name match -> setCombatTarget (ONLY targets, never pulls)

The gap: When isInCombat is true and user clicks an enemy name that matches an availableEnemy (not in fight), nothing happens because:
1. The !isInCombat guard (line 1383) blocks the pull path
2. The in-combat section (line 1412) only checks combatEnemiesList, not availableEnemies
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mid-combat pull via enemy name click in clickNpcKeyword</name>
  <files>src/App.vue</files>
  <action>
    In `src/App.vue`, modify the `clickNpcKeyword` handler (around line 1394-1420, the `isInCombat` block).

    **Before the existing in-combat enemy targeting code (line 1411-1419), add a check for available enemies that are NOT in the current fight:**

    Inside the `if (isInCombat.value)` block, BEFORE the combatEnemiesList matching (line 1411), add:

    ```typescript
    // Mid-combat pull: if clicked enemy is available (not in fight), initiate pull
    const pullableSpawn = availableEnemies.value.find(
      (e: any) => e.name?.toLowerCase() === kwLower
    );
    if (pullableSpawn) {
      startPull(pullableSpawn.id, 'body');
      return;
    }
    ```

    This must go AFTER the flee check (line 1397-1400) and AFTER the ability name match (line 1402-1410), but BEFORE the combatEnemiesList target match (line 1411-1419).

    The logic order inside the `if (isInCombat.value)` block should be:
    1. Flee keyword check (existing)
    2. Ability name match (existing)
    3. **NEW: Available enemy match -> startPull** (new code)
    4. Combat enemy match -> setCombatTarget (existing)

    This way: if an enemy name matches something already in combat, it targets it. If it matches something NOT in combat (available to pull), it pulls it. If the name appears in both lists (unlikely but possible), the pull takes priority since it's checked first -- but actually re-reading, we want targeting to take priority for enemies already in the fight. So swap the order:

    1. Flee keyword check (existing)
    2. Ability name match (existing)
    3. Combat enemy match -> setCombatTarget (existing, keep at line 1411)
    4. **NEW: Available enemy match -> startPull** (add AFTER combat enemy match)

    This way clicking an enemy already in your fight targets it, and clicking an enemy NOT in your fight pulls it.

    **Do NOT modify:**
    - The 'careful pull' / 'charge in' keyword handlers (lines 1365-1379) -- these already work
    - The !isInCombat enemy click section (lines 1383-1392) -- this is the out-of-combat path
    - useContextActions.ts -- the context action bar is correctly hidden during combat; pulls during combat are initiated via narrative clicks, not the action bar
    - NarrativeInput.vue -- no changes needed; the combat UI correctly shows combat-specific controls
  </action>
  <verify>
    <automated>cd C:/projects/uwr && npx vue-tsc --noEmit 2>&1 | head -20 || echo "Type check done"</automated>
  </verify>
  <done>Clicking an enemy name in narrative text during combat initiates a pull if the enemy is available (not in fight), or targets it if already in the fight</done>
</task>

</tasks>

<verification>
- During combat, clicking narrative enemy name that is in the fight -> targets it (existing behavior)
- During combat, clicking narrative enemy name that is NOT in the fight -> pulls it (new behavior)
- Outside combat, clicking enemy name -> pull flow (existing behavior unchanged)
- vue-tsc passes without type errors
</verification>

<success_criteria>
- Mid-combat enemy clicks route to startPull for available enemies
- Existing combat targeting for in-fight enemies is preserved
- No changes to server-side code (server already supports mid-combat pulls from 33-02)
</success_criteria>

<output>
After completion, create `.planning/phases/33-combat-improvements/33-05-SUMMARY.md`
</output>
