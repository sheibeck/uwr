---
phase: 30-narrative-combat
plan: 04
type: execute
wave: 3
depends_on: [30-02, 30-03]
files_modified:
  - src/module_bindings/
  - spacetimedb/src/reducers/intent.ts
  - src/composables/useNarrativeAnimation.ts
  - src/App.vue
autonomous: false
requirements: [COMBAT-01, COMBAT-02, COMBAT-03]

must_haves:
  truths:
    - "Full combat encounter works end-to-end: initiate -> action select -> resolve -> narrate -> victory/defeat"
    - "LLM narration appears in narrative stream with sardonic System voice during combat"
    - "Mechanical messages appear instantly; narration arrives async without blocking next round"
    - "Client bindings are regenerated and all components use the new table types"
    - "Intent service routes 'attack [name]' to combat initiation"
  artifacts:
    - path: "src/module_bindings/"
      provides: "Regenerated bindings with CombatRound, CombatAction, CombatNarrative types"
    - path: "spacetimedb/src/reducers/intent.ts"
      provides: "Attack intent routing to combat initiation"
  key_links:
    - from: "intent.ts attack handler"
      to: "startPull/startCombat reducers"
      via: "natural language 'attack X' routes to combat initiation"
      pattern: "attack"
    - from: "App.vue"
      to: "useCombat composable"
      via: "wires combat state to NarrativeConsole props or provides"
      pattern: "useCombat"
---

<objective>
Final integration: regenerate client bindings, wire combat state through App.vue, update intent service for attack commands, and verify the full combat flow end-to-end. This plan ensures all pieces from plans 01-03 connect and the game is playable.

Purpose: Plans 01-03 built the engine, narration, and UI separately. This plan wires them together, regenerates bindings so the client sees new tables, and adds the final integration points (intent service, App.vue wiring).

Output: Fully working narrative combat from start to finish, verified by playing through a combat encounter.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-narrative-combat/30-CONTEXT.md
@.planning/phases/30-narrative-combat/30-01-SUMMARY.md
@.planning/phases/30-narrative-combat/30-02-SUMMARY.md
@.planning/phases/30-narrative-combat/30-03-SUMMARY.md
@src/App.vue
@spacetimedb/src/reducers/intent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish, regenerate bindings, wire App.vue, update intent service</name>
  <files>src/module_bindings/, spacetimedb/src/reducers/intent.ts, src/App.vue, src/composables/useNarrativeAnimation.ts</files>
  <action>
1. **Publish module to local SpacetimeDB:**
   - Run `spacetime publish uwr -p spacetimedb --clear-database -y` (clear needed for schema changes)
   - Verify no publish errors

2. **Regenerate client bindings:**
   - Run `spacetime generate --lang typescript --out-dir src/module_bindings -p spacetimedb`
   - Verify new types appear: CombatRound, CombatAction, CombatNarrative

3. **Update intent.ts** for combat attack routing:
   - Add handling for 'attack [target]' or 'attack' intent
   - When player types "attack dire wolf" or clicks an enemy name from look output:
     - Parse the target name from the message
     - Find matching enemy spawn at the player's location
     - If found, initiate the pull prompt (send a system message asking "Approach carefully or charge in?" with [Careful Pull] and [Charge In] clickable options)
     - The actual pull reducers (start_pull / start_combat_with_enemy) are called when the player clicks the pull type option
   - If the intent service already handles 'attack' routing, update it to work with the narrative prompt approach

4. **Wire App.vue:**
   - Ensure useCombat composable is initialized and its state is passed to NarrativeConsole (or provided via inject/props)
   - Pass combat state: currentRound, roundState, actionPromptMessage, roundTimeRemaining, isInCombat
   - Ensure CombatPanel is no longer imported or rendered (should be done in Plan 03, but verify)
   - Subscribe to new tables in the subscription query: add 'SELECT * FROM combat_round', 'SELECT * FROM combat_action', 'SELECT * FROM combat_narrative' to the subscription builder

5. **Update useNarrativeAnimation.ts:**
   - Ensure 'combat_narration' events trigger typewriter animation
   - The animation should work the same as for 'narrative' and 'llm' kinds -- sentence-by-sentence reveal

6. **Fix any TypeScript compilation errors** from the new bindings and wiring:
   - Import new table types where needed
   - Update any type references from the old CombatPanel-based types to new round-based types
   - Ensure all reducers are called with correct object syntax

7. **Handle the combat keyword routing in the narrative console keyword handler:**
   - When window.clickNpcKeyword receives a combat-related keyword:
     - Ability names (e.g., "Void Rend"): submit ability action against current target
     - Enemy names (e.g., "Dire Wolf"): set as target, then show ability selection
     - "Auto-attack": submit auto-attack
     - "Flee": submit flee action
     - "More abilities...": show full ability list as a new prompt
     - "Careful Pull", "Charge In": start pull with appropriate type
     - "Take [ItemName]", "Take All Loot", "Dismiss": call appropriate loot reducer
   - This routing may live in NarrativeConsole.vue or a dedicated useCombatKeywords composable
  </action>
  <verify>
    <automated>cd C:/projects/uwr && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Module published, bindings regenerated, App.vue wires combat state, intent service handles attack commands, keyword handler routes combat actions, TypeScript compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full narrative combat flow end-to-end</name>
  <files>N/A</files>
  <action>
Complete narrative combat system: round-based engine, LLM narration, narrative stream UI, no floating panels. Full flow from initiating combat through action selection, round resolution with mechanical messages, LLM narration, and victory/defeat. Human verification required.
  </action>
  <verify>
    1. Start local SpacetimeDB: `spacetime start`
    2. Start client: `cd C:/projects/uwr && npm run dev`
    3. Log in and select/create a character
    4. Navigate to a location with enemies (use 'look' to see them)
    5. Click an enemy name or type 'attack [enemy name]'
    6. Verify: "Approach carefully or charge in?" prompt appears with [Careful Pull] and [Charge In] options
    7. Click [Charge In] (or [Careful Pull])
    8. Verify: Combat starts, action selection prompt appears with abilities, targets, auto-attack, and flee options
    9. Verify: Context action bar (explore, NPCs) is hidden during combat
    10. Click an ability, then a target (or type ability name)
    11. Verify: Action submitted, round resolves when timer expires or all submit
    12. Verify: Mechanical messages (damage, effects) appear INSTANTLY in the narrative stream
    13. Verify: Round summary with textual HP bars appears after resolution
    14. Verify: If qualifying round (crit/kill/near-death), LLM narration appears in gold/amber with typewriter animation
    15. Verify: Next round's action selection prompt appears immediately (not blocked by narration)
    16. Continue fighting until victory or defeat
    17. Verify: Victory/defeat narration appears with sardonic System voice
    18. Verify: Loot options appear as clickable text (if victory with loot)
    19. Verify: No floating CombatPanel appears at any point
    20. Verify: The System narrator maintains sardonic, dry voice throughout
  </verify>
  <done>Human has verified: full combat flow works, narration appears with sardonic voice, no floating panels, mechanical messages instant, narration async. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
- Full combat encounter playable from initiation to conclusion
- LLM narration appears for qualifying rounds with sardonic voice
- No floating combat panels
- All combat interaction through narrative stream
- Mechanical messages instant, narration async
- TypeScript compiles, module publishes
</verification>

<success_criteria>
- COMBAT-01: Combat rounds narrated by LLM over round-based engine -- verified via playtest
- COMBAT-02: Mechanical changes instant, narration async -- visible timing difference during combat
- COMBAT-03: Sardonic System narrator voice in combat descriptions -- verified in LLM output
- No regressions: non-combat gameplay (travel, NPCs, inventory) still works
</success_criteria>

<output>
After completion, create `.planning/phases/30-narrative-combat/30-04-SUMMARY.md`
</output>
