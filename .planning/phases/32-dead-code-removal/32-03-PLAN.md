---
phase: 32-dead-code-removal
plan: 03
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - src/components/CharacterPanel.vue
  - src/components/NpcDialogPanel.vue
  - src/components/QuestPanel.vue
  - src/components/RenownPanel.vue
  - src/composables/useCharacterCreation.ts
  - src/App.vue
autonomous: true
requirements:
  - CLEAN-04
  - CLEAN-06
must_haves:
  truths:
    - "All legacy panel components are deleted"
    - "useCharacterCreation composable is deleted"
    - "App.vue has no imports or references to deleted components"
    - "Frontend compiles and renders without errors"
    - "No orphaned composables or components remain after cleanup"
  artifacts:
    - path: "src/components/CharacterPanel.vue"
      provides: "File must NOT exist"
    - path: "src/components/NpcDialogPanel.vue"
      provides: "File must NOT exist"
    - path: "src/components/QuestPanel.vue"
      provides: "File must NOT exist"
    - path: "src/components/RenownPanel.vue"
      provides: "File must NOT exist"
    - path: "src/composables/useCharacterCreation.ts"
      provides: "File must NOT exist"
  key_links:
    - from: "src/App.vue"
      to: "src/components/Narrative*.vue"
      via: "Only narrative components remain as active UI"
      pattern: "import.*Narrative"
---

<objective>
Remove all legacy frontend panels (CharacterPanel, NpcDialogPanel, QuestPanel, RenownPanel), their associated composables, and clean App.vue of all dead references. Trace the full import graph to find any other orphaned components or composables.

Purpose: The narrative UI built in v2.0 replaced all legacy panels. Removing them eliminates confusion about which UI layer is active and reduces the frontend codebase size.

Output: 4 components deleted, 1+ composables deleted, App.vue cleaned of dead code.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dead-code-removal/32-CONTEXT.md
@.planning/phases/32-dead-code-removal/32-RESEARCH.md

Key files to understand current state:
@src/App.vue
@src/components/CharacterPanel.vue
@src/components/NpcDialogPanel.vue
@src/components/QuestPanel.vue
@src/components/RenownPanel.vue
@src/composables/useCharacterCreation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy panels and composables</name>
  <files>
    src/components/CharacterPanel.vue,
    src/components/NpcDialogPanel.vue,
    src/components/QuestPanel.vue,
    src/components/RenownPanel.vue,
    src/composables/useCharacterCreation.ts
  </files>
  <action>
    1. Delete the 4 legacy panel components:
       - `rm src/components/CharacterPanel.vue`
       - `rm src/components/NpcDialogPanel.vue`
       - `rm src/components/QuestPanel.vue`
       - `rm src/components/RenownPanel.vue`

    2. Delete the legacy composable:
       - `rm src/composables/useCharacterCreation.ts`

    3. Trace the full frontend import graph to find ALL other orphaned files:
       - List all components: `ls src/components/*.vue`
       - List all composables: `ls src/composables/*.ts`
       - For each non-Narrative component and composable, grep for imports across the codebase: `grep -r "ComponentName\|composableName" src/ --include="*.vue" --include="*.ts"`
       - If a component/composable is ONLY imported by a file that was just deleted, it is also orphaned -- delete it too
       - Repeat until no more orphans are found

    4. Check for any other files in the MEMORY.md deletion list that are in the frontend:
       - `src/components/CharacterPanel.vue` (old form-based creation) -- delete
       - `useCharacterCreation.ts` -- delete
       - Any other composables only used by deleted panels
  </action>
  <verify>
    <automated>ls C:/projects/uwr/src/components/CharacterPanel.vue C:/projects/uwr/src/components/NpcDialogPanel.vue C:/projects/uwr/src/components/QuestPanel.vue C:/projects/uwr/src/components/RenownPanel.vue C:/projects/uwr/src/composables/useCharacterCreation.ts 2>&1</automated>
  </verify>
  <done>All 4 legacy panels deleted. useCharacterCreation deleted. Any additional orphaned components/composables also deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Clean App.vue and verify frontend compilation</name>
  <files>src/App.vue</files>
  <action>
    App.vue is ~2971 lines and contains accumulated references to deleted panels. Clean it thoroughly:

    1. **Remove imports** of deleted components and composables:
       - Remove NpcDialogPanel import (line ~545) and template usage (line ~154)
       - Remove RenownPanel import (line ~549) and template usage (line ~159)
       - Remove useCharacterCreation import (line ~561) and all destructured values (line ~870)
       - Remove CharacterPanel import if present
       - Remove QuestPanel import if present

    2. **Remove template references** to deleted components:
       - Search for `<CharacterPanel`, `<NpcDialogPanel`, `<QuestPanel`, `<RenownPanel` in template section
       - Remove entire component tags and any v-if/v-show wrappers that only contained deleted components

    3. **Remove orphaned script code**:
       - After removing imports, search for variables/refs/computed properties that were ONLY used by deleted panels
       - Common patterns: `showCharacterPanel`, `showNpcDialog`, `showQuestPanel`, `showRenownPanel` refs
       - Event handlers like `onCreateCharacter`, `onNpcDialogClose`, etc. that only served deleted panels
       - Data fetching or watch expressions that only populated deleted panel props
       - Remove ALL dead code -- but be careful not to remove variables used by BOTH deleted panels AND active narrative components

    4. **Clean up any orphaned event handlers**:
       - Search for `@` event bindings in template that reference removed handlers
       - Search for `emit` calls that were only received by deleted components

    5. **Verify frontend builds**:
       - Run `cd C:/projects/uwr && npm run build` (or the appropriate build command)
       - Fix any compilation errors
       - If TypeScript errors appear about missing components, those are expected -- just remove the references

    **IMPORTANT:** App.vue is large. Read the ENTIRE file before making changes. Make a systematic pass -- do not leave orphaned code.
  </action>
  <verify>
    <automated>cd C:/projects/uwr && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>App.vue is free of all references to deleted components and composables. No orphaned variables, handlers, or computed properties remain. Frontend compiles cleanly with no errors.</done>
</task>

</tasks>

<verification>
- `npm run build` in project root -- zero errors
- `grep -r "CharacterPanel\|NpcDialogPanel\|QuestPanel\|RenownPanel\|useCharacterCreation" src/ --include="*.vue" --include="*.ts"` -- no results
- All deleted files confirmed absent
</verification>

<success_criteria>
- 4 legacy panel components deleted
- useCharacterCreation composable deleted
- Any additional orphaned components/composables identified and deleted
- App.vue has zero references to deleted files
- Frontend compiles cleanly
- No broken imports in the frontend
</success_criteria>

<output>
After completion, create `.planning/phases/32-dead-code-removal/32-03-SUMMARY.md`
</output>
