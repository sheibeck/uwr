---
phase: 32-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/data/equipment_rules.ts
  - spacetimedb/src/data/crafting_rules.ts
  - spacetimedb/src/data/enemy_rules.ts
  - spacetimedb/src/data/faction_rules.ts
  - spacetimedb/src/data/npc_rules.ts
  - spacetimedb/src/helpers/world_gen.ts
  - spacetimedb/src/helpers/scheduling.ts
  - spacetimedb/src/helpers/location.ts
  - spacetimedb/src/helpers/items.ts
  - spacetimedb/src/helpers/npc_affinity.ts
  - spacetimedb/src/reducers/combat.ts
  - spacetimedb/src/reducers/items_gathering.ts
  - spacetimedb/src/reducers/items_crafting.ts
  - spacetimedb/src/index.ts
  - spacetimedb/src/reducers/commands.ts
  - spacetimedb/src/reducers/items.ts
autonomous: true
requirements:
  - CLEAN-02
  - CLEAN-06
must_haves:
  truths:
    - "All implicit mechanical rules from legacy data files are preserved in domain-specific rule files"
    - "All active importers of legacy data files now import from new rule files instead"
    - "Active world_gen functions are relocated and still callable"
    - "Scheduled table setup is relocated out of seeding and still functional"
    - "Backend compiles and all tests pass with new import paths"
  artifacts:
    - path: "spacetimedb/src/data/equipment_rules.ts"
      provides: "Armor class restrictions, stat ranges, weapon type constraints, starter weapon defs"
      exports: ["ARMOR_ALLOWED_CLASSES", "STARTER_WEAPON_DEFS"]
    - path: "spacetimedb/src/data/crafting_rules.ts"
      provides: "Material tiers, modifier defs, crafting formulas, salvage rules"
      exports: ["MATERIAL_DEFS", "CRAFTING_MODIFIER_DEFS", "ESSENCE_TIER_THRESHOLDS"]
    - path: "spacetimedb/src/data/enemy_rules.ts"
      provides: "Boss stat scaling, ability slot counts, loot drop rate ranges"
    - path: "spacetimedb/src/data/faction_rules.ts"
      provides: "Rival relationship structure, faction type vocabulary"
    - path: "spacetimedb/src/data/npc_rules.ts"
      provides: "Affinity tier constants, conversation cooldowns"
      exports: ["AFFINITY_TIERS", "CONVERSATION_COOLDOWN_MICROS"]
    - path: "spacetimedb/src/helpers/scheduling.ts"
      provides: "Scheduled table setup functions relocated from ensure_content.ts"
  key_links:
    - from: "spacetimedb/src/helpers/location.ts"
      to: "spacetimedb/src/data/crafting_rules.ts"
      via: "import MATERIAL_DEFS, CRAFTING_MODIFIER_DEFS"
      pattern: "from.*crafting_rules"
    - from: "spacetimedb/src/reducers/items_crafting.ts"
      to: "spacetimedb/src/data/crafting_rules.ts"
      via: "import crafting helpers and constants"
      pattern: "from.*crafting_rules"
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/helpers/scheduling.ts"
      via: "import scheduled table init replacing syncAllContent"
      pattern: "from.*helpers/scheduling"
---

<objective>
Extract all implicit mechanical rules from v1.0 legacy data files into domain-specific rule files and rewire all importers to use the new locations. Relocate active functions from world_gen.ts and scheduled table setup from ensure_content.ts.

Purpose: This is the prerequisite for safe deletion -- every active export from legacy files must have a new home before the files can be removed. Without this, deletion would break compilation.

Output: 5 new domain rule files (equipment_rules.ts, crafting_rules.ts, enemy_rules.ts, faction_rules.ts, npc_rules.ts), updated helpers/world_gen.ts with relocated functions, new helpers/scheduling.ts with scheduled table init.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dead-code-removal/32-CONTEXT.md
@.planning/phases/32-dead-code-removal/32-RESEARCH.md

Source files to extract FROM (read these to understand what to extract):
@spacetimedb/src/data/item_defs.ts
@spacetimedb/src/data/crafting_materials.ts
@spacetimedb/src/data/named_enemy_defs.ts
@spacetimedb/src/data/faction_data.ts
@spacetimedb/src/data/npc_data.ts
@spacetimedb/src/data/world_gen.ts
@spacetimedb/src/seeding/ensure_content.ts

Files that IMPORT from legacy files (must be rewired):
@spacetimedb/src/helpers/location.ts
@spacetimedb/src/helpers/items.ts
@spacetimedb/src/helpers/npc_affinity.ts
@spacetimedb/src/helpers/world_gen.ts
@spacetimedb/src/reducers/combat.ts
@spacetimedb/src/reducers/items_gathering.ts
@spacetimedb/src/reducers/items_crafting.ts
@spacetimedb/src/index.ts
@spacetimedb/src/reducers/commands.ts
@spacetimedb/src/reducers/items.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create domain rule files and relocate active functions</name>
  <files>
    spacetimedb/src/data/equipment_rules.ts,
    spacetimedb/src/data/crafting_rules.ts,
    spacetimedb/src/data/enemy_rules.ts,
    spacetimedb/src/data/faction_rules.ts,
    spacetimedb/src/data/npc_rules.ts,
    spacetimedb/src/helpers/world_gen.ts,
    spacetimedb/src/helpers/scheduling.ts
  </files>
  <action>
    Read each legacy data file in full. Create new domain rule files extracting ONLY mechanical rules, discarding specific seeded content:

    **equipment_rules.ts** (from item_defs.ts):
    - KEEP: ARMOR_ALLOWED_CLASSES mapping, StarterWeaponDef interface + STARTER_WEAPON_DEFS array, WorldDropItemDef interface (if referenced by non-deleted code)
    - DISCARD: All specific gear definitions (WORLD_DROP_GEAR_DEFS, WORLD_DROP_JEWELRY_DEFS, CRAFTING_BASE_GEAR_DEFS, BOSS_DROP_DEFS, starter accessory/armor descriptions, junk defs, resource defs)

    **crafting_rules.ts** (from crafting_materials.ts):
    - KEEP: MaterialDef interface + MATERIAL_DEFS, CraftingModifierDef interface + CRAFTING_MODIFIER_DEFS, CRAFTING_MODIFIER_WEIGHT_MULTIPLIER, all helper functions (materialTierToCraftQuality, getMaterialForSalvage, getCraftQualityStatBonus, getModifierMagnitude), CRAFT_QUALITY_LEVELS, AFFIX_SLOTS_BY_QUALITY, ESSENCE_MAGNITUDE, ESSENCE_QUALITY_GATE, SALVAGE_YIELD_BY_TIER, ESSENCE_TIER_THRESHOLDS, MODIFIER_REAGENT_THRESHOLDS, MODIFIER_MAGNITUDE_BY_ESSENCE
    - DISCARD: ConsumableRecipeDef + CONSUMABLE_RECIPES, GearRecipeDef + GEAR_RECIPES, GEAR_RECIPE_NAMES (ONLY if these are not imported by any live reducers -- check items_crafting.ts imports first. If they ARE imported by live code, KEEP them)

    **enemy_rules.ts** (from named_enemy_defs.ts):
    - KEEP: NamedEnemyLootDef interface (loot table structure), any boss stat scaling formulas/constants. Document scaling patterns as exported constants/formulas
    - DISCARD: All 12 specific NAMED_ENEMY_DEFS entries, BOSS_DROP_DEFS

    **faction_rules.ts** (from faction_data.ts):
    - KEEP: Rival relationship structure (faction has rivalFactionId concept), faction type enum/vocabulary if any
    - DISCARD: FACTION_DATA array, ensureFactions function

    **npc_rules.ts** (from npc_data.ts):
    - KEEP: AFFINITY_TIERS, AFFINITY_TIER_NAMES, getAffinityTierName, CONVERSATION_COOLDOWN_MICROS, MAX_GIFTS_PER_DAY, GIFT_COOLDOWN_MICROS
    - DISCARD: NPC_PERSONALITIES

    **Relocate world_gen functions** (from data/world_gen.ts to helpers/world_gen.ts):
    - Move pickRippleMessage, pickDiscoveryMessage, computeRegionDanger and their template arrays (RIPPLE_TEMPLATES, DISCOVERY_TEMPLATES, BIOME_HINTS) into helpers/world_gen.ts
    - Merge with existing helpers/world_gen.ts content (it already exists and imports from data/world_gen.ts)

    **Create helpers/scheduling.ts** (from seeding/ensure_content.ts):
    - Read ensure_content.ts in full
    - Extract ONLY the scheduled table setup functions: ensureHealthRegenScheduled, ensureDayNightTickScheduled, and any other ensureXxxScheduled functions
    - Create a new initScheduledTables(ctx) function that calls all of them
    - Do NOT include any seeding functions (ensureItems, ensureEnemies, ensureWorld, etc.)
  </action>
  <verify>
    <automated>cd C:/projects/uwr/spacetimedb && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 5 domain rule files exist with correct exports. helpers/world_gen.ts contains relocated functions. helpers/scheduling.ts contains scheduled table init. Files may have compilation errors from unrewired imports (fixed in Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Rewire all importers to use new rule files and locations</name>
  <files>
    spacetimedb/src/helpers/location.ts,
    spacetimedb/src/helpers/items.ts,
    spacetimedb/src/helpers/npc_affinity.ts,
    spacetimedb/src/reducers/combat.ts,
    spacetimedb/src/reducers/items_gathering.ts,
    spacetimedb/src/reducers/items_crafting.ts,
    spacetimedb/src/index.ts,
    spacetimedb/src/reducers/commands.ts,
    spacetimedb/src/reducers/items.ts
  </files>
  <action>
    Update every file that imports from legacy data files to import from the new domain rule files instead:

    **From crafting_materials.ts (6 active importers):**
    - helpers/location.ts: Change import to `from '../data/crafting_rules'` for MATERIAL_DEFS, CRAFTING_MODIFIER_DEFS, CRAFTING_MODIFIER_WEIGHT_MULTIPLIER
    - reducers/combat.ts: Change import to `from '../data/crafting_rules'` for ESSENCE_TIER_THRESHOLDS, MODIFIER_REAGENT_THRESHOLDS, CRAFTING_MODIFIER_DEFS
    - reducers/items_gathering.ts: Change import to `from '../data/crafting_rules'` for CRAFTING_MODIFIER_DEFS
    - reducers/items_crafting.ts: Change import to `from '../data/crafting_rules'` for ALL crafting imports (getMaterialForSalvage, SALVAGE_YIELD_BY_TIER, MATERIAL_DEFS, materialTierToCraftQuality, getCraftQualityStatBonus, CRAFTING_MODIFIER_DEFS, AFFIX_SLOTS_BY_QUALITY, ESSENCE_MAGNITUDE, ESSENCE_QUALITY_GATE, getModifierMagnitude)

    **From item_defs.ts (1 active importer):**
    - helpers/items.ts: Change STARTER_WEAPON_DEFS import to `from '../data/equipment_rules'`

    **From npc_data.ts (1 active importer):**
    - helpers/npc_affinity.ts: Change CONVERSATION_COOLDOWN_MICROS (and any other npc_data imports) to `from '../data/npc_rules'`

    **From faction_data.ts (1 active importer):**
    - index.ts: Remove the ensureFactions import and its call site entirely (factions are now generated through play)

    **From data/world_gen.ts (2 active importers):**
    - index.ts: Change imports of pickRippleMessage, pickDiscoveryMessage, computeRegionDanger to `from './helpers/world_gen'`
    - helpers/world_gen.ts: Remove imports from `../data/world_gen` since functions are now local

    **syncAllContent replacement:**
    - index.ts: Replace `syncAllContent(ctx)` call in clientConnected with `initScheduledTables(ctx)` from `./helpers/scheduling`
    - reducers/items.ts: Remove syncAllContent call (line ~996) -- scheduled tables are init'd on connect, not per-item
    - reducers/commands.ts: Replace syncAllContent call (line ~270) in admin resync command with `initScheduledTables(ctx)` import from `../helpers/scheduling`

    **Update test file mock paths:**
    - If helpers/world_gen.test.ts mocks `../data/world_gen`, update to mock the local functions or remove the mock since functions are now in the same file
    - If helpers/items.test.ts mocks item_defs imports, update mock path to `../data/equipment_rules`

    After all rewiring, run `cd spacetimedb && npm test` to confirm existing tests still pass with new import paths.
  </action>
  <verify>
    <automated>cd C:/projects/uwr/spacetimedb && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>All legacy data file imports are replaced with new domain rule file imports. syncAllContent calls replaced with initScheduledTables. Backend compiles cleanly. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `cd spacetimedb && npx tsc --noEmit` -- zero errors
- `cd spacetimedb && npm test` -- all tests pass
- grep for old import paths confirms no remaining references: `grep -r "from.*crafting_materials\|from.*item_defs\|from.*named_enemy_defs\|from.*faction_data\|from.*npc_data\|from.*data/world_gen\|from.*seeding/" spacetimedb/src/ --include="*.ts" | grep -v "node_modules" | grep -v ".test."`
</verification>

<success_criteria>
- 5 domain rule files exist in spacetimedb/src/data/ with all active mechanical rules extracted
- helpers/scheduling.ts exists with initScheduledTables function
- helpers/world_gen.ts contains relocated generation functions
- Zero import references to legacy data files remain in non-test production code
- All Phase 31 tests pass (regression guard)
- Backend compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/32-dead-code-removal/32-01-SUMMARY.md`
</output>
