---
phase: 32-dead-code-removal
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - spacetimedb/src/data/dialogue_data.ts
  - spacetimedb/src/data/faction_data.ts
  - spacetimedb/src/data/npc_data.ts
  - spacetimedb/src/data/named_enemy_defs.ts
  - spacetimedb/src/data/item_defs.ts
  - spacetimedb/src/data/crafting_materials.ts
  - spacetimedb/src/data/world_gen.ts
  - spacetimedb/src/seeding/ensure_content.ts
  - spacetimedb/src/seeding/ensure_enemies.ts
  - spacetimedb/src/seeding/ensure_items.ts
  - spacetimedb/src/seeding/ensure_world.ts
  - spacetimedb/src/reducers/characters.ts
  - spacetimedb/src/index.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03
  - CLEAN-05
must_haves:
  truths:
    - "All v1.0 legacy data files from the MEMORY.md deletion list are gone"
    - "The entire seeding directory is removed"
    - "The old create_character reducer is removed"
    - "Backend compiles and all tests pass after deletions"
    - "No dead reducers or unused table accessors remain"
  artifacts:
    - path: "spacetimedb/src/seeding/"
      provides: "Directory must NOT exist"
    - path: "spacetimedb/src/data/dialogue_data.ts"
      provides: "File must NOT exist"
    - path: "spacetimedb/src/data/item_defs.ts"
      provides: "File must NOT exist"
    - path: "spacetimedb/src/data/crafting_materials.ts"
      provides: "File must NOT exist"
  key_links:
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/helpers/scheduling.ts"
      via: "initScheduledTables replaces syncAllContent"
      pattern: "initScheduledTables"
---

<objective>
Delete all v1.0 legacy backend files (seeding system, data files on the MEMORY.md deletion list), remove dead reducers, and identify/extract any duplicated backend logic into shared helpers.

Purpose: Enforce the v2.0 principle that "everything is generated through play, nothing is pre-seeded" by physically removing the seeding infrastructure and legacy data definitions. Also deduplicate backend logic (CLEAN-03).

Output: ~11 files deleted, dead reducers removed, any duplicated sell/economy/combat logic consolidated into shared helpers.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dead-code-removal/32-CONTEXT.md
@.planning/phases/32-dead-code-removal/32-RESEARCH.md
@.planning/phases/32-dead-code-removal/32-01-SUMMARY.md

Key files to understand what was already rewired in Plan 01:
@spacetimedb/src/index.ts
@spacetimedb/src/reducers/characters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete legacy data files and seeding system</name>
  <files>
    spacetimedb/src/seeding/ensure_content.ts,
    spacetimedb/src/seeding/ensure_enemies.ts,
    spacetimedb/src/seeding/ensure_items.ts,
    spacetimedb/src/seeding/ensure_world.ts,
    spacetimedb/src/data/dialogue_data.ts,
    spacetimedb/src/data/faction_data.ts,
    spacetimedb/src/data/npc_data.ts,
    spacetimedb/src/data/named_enemy_defs.ts,
    spacetimedb/src/data/item_defs.ts,
    spacetimedb/src/data/crafting_materials.ts,
    spacetimedb/src/data/world_gen.ts,
    spacetimedb/src/index.ts,
    spacetimedb/src/reducers/characters.ts
  </files>
  <action>
    Delete all legacy files in this exact order (Plan 01 already rewired all active importers):

    1. Delete the entire seeding directory: `rm -rf spacetimedb/src/seeding/`
    2. Delete data files one by one:
       - `rm spacetimedb/src/data/dialogue_data.ts`
       - `rm spacetimedb/src/data/faction_data.ts`
       - `rm spacetimedb/src/data/npc_data.ts`
       - `rm spacetimedb/src/data/named_enemy_defs.ts`
       - `rm spacetimedb/src/data/item_defs.ts`
       - `rm spacetimedb/src/data/crafting_materials.ts`
       - `rm spacetimedb/src/data/world_gen.ts`

    3. Remove dead code from remaining files:
       - In `reducers/characters.ts`: Remove the old `create_character` reducer (the form-based v1.0 character creation). Keep the narrative-based character creation flow
       - In `index.ts`: Remove any remaining imports from deleted files. Remove the ensureFactions call if not already removed in Plan 01. Remove syncAllContent import if not already removed. Remove any table definitions that were ONLY used by seeding code (scan for tables with no active reducer or view references)
       - Search ALL reducer files for dead imports from deleted files: `grep -r "from.*seeding\|from.*dialogue_data\|from.*faction_data\|from.*npc_data\|from.*named_enemy_defs\|from.*item_defs\|from.*crafting_materials\|from.*data/world_gen" spacetimedb/src/ --include="*.ts"` -- fix any remaining references

    4. Compile check: `cd spacetimedb && npx tsc --noEmit` -- fix ANY errors before proceeding

    **CRITICAL:** If compilation reveals imports that Plan 01 missed, fix them by either:
    - Importing from the appropriate new rule file (if the export was extracted)
    - Removing the import if the functionality is no longer needed
    Do NOT skip compilation errors.
  </action>
  <verify>
    <automated>cd C:/projects/uwr && ls spacetimedb/src/seeding/ 2>&1 && ls spacetimedb/src/data/dialogue_data.ts spacetimedb/src/data/item_defs.ts spacetimedb/src/data/crafting_materials.ts 2>&1 ; cd spacetimedb && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 data files and 4 seeding files are deleted. Old create_character reducer removed. Backend compiles with zero errors. No remaining imports from deleted files.</done>
</task>

<task type="auto">
  <name>Task 2: Deduplicate backend logic and remove dead reducers</name>
  <files>
    spacetimedb/src/helpers/economy.ts,
    spacetimedb/src/helpers/combat.ts,
    spacetimedb/src/helpers/items.ts,
    spacetimedb/src/reducers/items.ts,
    spacetimedb/src/reducers/npc_interaction.ts,
    spacetimedb/src/reducers/quests.ts,
    spacetimedb/src/reducers/intent.ts,
    spacetimedb/src/index.ts
  </files>
  <action>
    **Step 1: Identify all backend duplication** (per user decision: "Claude identifies ALL backend duplication"):

    Search for duplicated patterns across reducer files:
    - Sell price calculation (base price * vendor multiplier * perk bonus) -- check items.ts, npc_interaction.ts, quests.ts, intent.ts
    - Gold math helpers (addGold, removeGold, canAfford) -- check all reducer files
    - Stat lookup patterns (getStatValue, computeEffectiveStat) -- check combat.ts and any other files
    - Damage formula patterns -- check if any combat math is duplicated outside helpers/combat.ts

    For each duplication found:
    - Extract the shared logic into the appropriate existing helper file:
      - Gold/sell/economy logic -> helpers/economy.ts (create if needed, or add to existing)
      - Combat utilities -> helpers/combat.ts
      - Item utilities -> helpers/items.ts
    - Update all call sites to use the shared helper
    - Do NOT create new helper files unless the function truly doesn't fit existing ones

    **Step 2: Remove dead reducers and unused table accessors** (CLEAN-05):
    - Search for reducers that are never called from the client or from other reducers
    - Remove any table accessor functions that only existed for seeding
    - Check if any tables in schema are now orphaned (only used by deleted seeding code) -- if so, remove them from the schema. Flag borderline cases with a comment
    - Remove unused table accessors from index.ts

    **Step 3: Final backend verification:**
    - Run `cd spacetimedb && npm test` to confirm all tests pass
    - Run `cd spacetimedb && npx tsc --noEmit` for clean compilation
  </action>
  <verify>
    <automated>cd C:/projects/uwr/spacetimedb && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>All duplicated sell/economy/combat logic consolidated into shared helpers. Dead reducers and unused table accessors removed. All tests pass. Backend compiles cleanly.</done>
</task>

</tasks>

<verification>
- `cd spacetimedb && npx tsc --noEmit` -- zero errors
- `cd spacetimedb && npm test` -- all tests pass
- `ls spacetimedb/src/seeding/` -- directory does not exist
- `ls spacetimedb/src/data/dialogue_data.ts spacetimedb/src/data/item_defs.ts spacetimedb/src/data/crafting_materials.ts spacetimedb/src/data/faction_data.ts spacetimedb/src/data/npc_data.ts spacetimedb/src/data/named_enemy_defs.ts spacetimedb/src/data/world_gen.ts 2>&1` -- all report "No such file"
- `grep -r "create_character" spacetimedb/src/reducers/characters.ts` -- no old reducer
</verification>

<success_criteria>
- All 11 files from the deletion list are gone (7 data files + 4 seeding files)
- Seeding directory does not exist
- Old create_character reducer is removed from characters.ts
- Duplicated backend logic is consolidated into shared helpers
- Dead reducers and unused table accessors are removed
- Backend compiles and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/32-dead-code-removal/32-02-SUMMARY.md`
</output>
