---
phase: 36-ability-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/data/races.ts
  - spacetimedb/src/helpers/combat_rewards.ts
  - spacetimedb/src/index.ts
  - spacetimedb/src/reducers/intent.ts
  - spacetimedb/src/helpers/combat_rewards.test.ts
autonomous: true
requirements: [ABIL-06, ABIL-07]

must_haves:
  truths:
    - "Heritage bonuses apply every level (not every other level)"
    - "Race display in character creation shows 'Level Bonus (every level)' not 'every 2 levels'"
    - "Level-up heritage notification fires every level (not just even levels)"
    - "Each predefined race in RACE_DATA has a functional race ability definition"
    - "Race ability values compensate for halved levelBonusValue (power parity with v1.0)"
  artifacts:
    - path: "spacetimedb/src/helpers/combat_rewards.ts"
      provides: "Every-level heritage bonus computation"
      contains: "raceRow.levelBonusValue \\* level"
    - path: "spacetimedb/src/data/races.ts"
      provides: "Race ability definitions and halved levelBonusValue"
      contains: "abilityKind.*abilityKey"
    - path: "spacetimedb/src/helpers/combat_rewards.test.ts"
      provides: "Tests for every-level heritage computation"
  key_links:
    - from: "spacetimedb/src/helpers/combat_rewards.ts"
      to: "spacetimedb/src/data/races.ts"
      via: "computeRacialAtLevelFromRow reads levelBonusValue"
      pattern: "levelBonusValue \\* level"
    - from: "spacetimedb/src/index.ts"
      to: "spacetimedb/src/helpers/combat_rewards.ts"
      via: "apply_level_up calls computeRacialAtLevelFromRow"
      pattern: "computeRacialAtLevelFromRow"
---

<objective>
Fix heritage bonuses to apply every level and add functional race ability definitions.

Purpose: Players gain heritage bonuses each level (not every other) matching the user's locked decision. Each race gets a real, functional ability (not just narrative text) that will be granted at character creation by plan 03's dispatch work.
Output: Updated combat_rewards.ts formula, halved levelBonusValue in races.ts, race ability data fields, updated display in intent.ts and index.ts.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-ability-expansion/36-RESEARCH.md

<interfaces>
<!-- computeRacialAtLevelFromRow in combat_rewards.ts uses: -->
```typescript
const evenLevels = level / 2n;
if (evenLevels > 0n) {
  applyType(raceRow.levelBonusType, raceRow.levelBonusValue * evenLevels);
}
```

<!-- Level-up heritage notification in index.ts: -->
```typescript
if (newLevel % 2n === 0n && raceRow) {
  appendPrivateEvent(..., `Your ${raceRow.name} heritage grows stronger at level ${newLevel}.`);
}
```

<!-- Intent races command shows: -->
```
Level Bonus (every 2 levels): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Change heritage bonus to every level and add race ability data</name>
  <files>spacetimedb/src/helpers/combat_rewards.ts, spacetimedb/src/data/races.ts, spacetimedb/src/helpers/combat_rewards.test.ts</files>
  <behavior>
    - Test: computeRacialAtLevelFromRow at level 5 applies levelBonusValue * 5 (not 5/2=2)
    - Test: computeRacialAtLevelFromRow at level 1 applies levelBonusValue * 1
    - Test: At level 10, the total heritage bonus equals levelBonusValue * 10 (previously was * 5)
    - Test: Each RACE_DATA entry has abilityKind, abilityName, abilityKey fields
  </behavior>
  <action>
    1. In combat_rewards.ts `computeRacialAtLevelFromRow`:
       - Remove the `const evenLevels = level / 2n;` line
       - Change the condition from `if (evenLevels > 0n)` to `if (level > 0n)`
       - Change the application from `raceRow.levelBonusValue * evenLevels` to `raceRow.levelBonusValue * level`

    2. In races.ts: HALVE the `levelBonusValue` for each race to maintain power parity:
       - Per the research pitfall 1: "Eldrin's levelBonusValue: 2n becomes 1n, so at level 20: 1 * 20 = 20 (same as 2 * 10 before)"
       - Go through each RACE_DATA entry and halve its levelBonusValue (round up if odd)

    3. In races.ts: Add race ability fields to each RACE_DATA entry (suggested abilities from research):
       ```typescript
       // Add to each entry:
       abilityName: string;
       abilityDescription: string;
       abilityKind: string;       // e.g. 'buff', 'hot', 'utility'
       abilityTargetRule: string;  // e.g. 'self'
       abilityCooldownSeconds: bigint; // 300n typical (5 min)
       abilityValue: bigint;
       abilityKey: string;        // e.g. 'race_troll_regen'
       ```
       Use the suggested abilities from the research (Diplomatic Poise for Human, Arcane Attunement for Eldrin, etc.) but adjust kinds to match the expanded ABILITY_KINDS from plan 01.

    4. Create combat_rewards.test.ts with tests for the new every-level formula.

    IMPORTANT: The levelBonusValue halving is critical to avoid power inflation for existing characters.
  </action>
  <verify>
    <automated>cd spacetimedb && npm test -- combat_rewards</automated>
  </verify>
  <done>Heritage bonus applies every level with halved values maintaining power parity. Each RACE_DATA entry has race ability fields.</done>
</task>

<task type="auto">
  <name>Task 2: Update heritage display in intent.ts and level-up in index.ts</name>
  <files>spacetimedb/src/reducers/intent.ts, spacetimedb/src/index.ts</files>
  <action>
    1. In intent.ts: Find the races command display where it shows "Level Bonus (every 2 levels)" and change to "Level Bonus (every level)". Also display the race ability info (name, description, cooldown) so players see it during character creation.

    2. In index.ts: Find the level-up heritage notification gate:
       ```typescript
       if (newLevel % 2n === 0n && raceRow) {
       ```
       Change to:
       ```typescript
       if (raceRow) {
       ```
       So the heritage message fires on every level-up, not just even ones.

    3. In index.ts: During the level-up flow, also show the heritage bonus amount. The message should say something like:
       "Your {race} heritage grows stronger — +{value} {bonusType} at level {level}."
  </action>
  <verify>
    <automated>cd spacetimedb && npx tsc --noEmit</automated>
  </verify>
  <done>Races command shows "every level" for heritage bonus. Level-up notification fires every level. Race ability info visible during character creation.</done>
</task>

</tasks>

<verification>
- `cd spacetimedb && npm test -- combat_rewards` passes
- `cd spacetimedb && npx tsc --noEmit` passes
- grep for "evenLevels" in combat_rewards.ts returns no results
- grep for "every 2 levels" in intent.ts returns no results
- grep for "newLevel % 2n" in index.ts returns no results (for heritage gate)
</verification>

<success_criteria>
Heritage bonuses apply every level with halved values (power parity). Race abilities are defined in RACE_DATA with kind, name, key, cooldown, and value. Display text updated in character creation and level-up.
</success_criteria>

<output>
After completion, create `.planning/phases/36-ability-expansion/36-02-SUMMARY.md`
</output>
