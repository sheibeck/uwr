---
phase: 034-narrative-ui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spacetimedb/src/reducers/intent.ts
  - spacetimedb/src/reducers/items.ts
  - src/components/NarrativeMessage.vue
  - src/components/NarrativeMessage.test.ts
  - spacetimedb/src/reducers/intent.test.ts
autonomous: true
requirements: [NARR-01, NARR-02, NARR-05]

must_haves:
  truths:
    - "Player typing 'sell <item>' gets correct gold including perk bonuses"
    - "Player typing 'sell all junk' sells all junk items with perk bonus and sees a summary"
    - "Player typing 'sell 3 <item>' sells up to 3 of that item with perk bonus"
    - "Event feed entries display correct colors by kind (combat=red, reward=gold, system=gray, social=blue)"
  artifacts:
    - path: "spacetimedb/src/reducers/intent.ts"
      provides: "sell perk fix, sell all junk command, sell N command"
      contains: "getPerkBonusByField"
    - path: "spacetimedb/src/reducers/items.ts"
      provides: "sell_all_junk reducer with perk bonus fix"
      contains: "getPerkBonusByField"
    - path: "src/components/NarrativeMessage.vue"
      provides: "Complete KIND_COLORS map with social entry"
      contains: "social"
    - path: "spacetimedb/src/reducers/intent.test.ts"
      provides: "Tests for sell commands"
    - path: "src/components/NarrativeMessage.test.ts"
      provides: "Tests for KIND_COLORS required keys and values"
  key_links:
    - from: "spacetimedb/src/reducers/intent.ts"
      to: "spacetimedb/src/helpers/economy.ts"
      via: "computeSellValue + getPerkBonusByField"
      pattern: "getPerkBonusByField.*vendorSellBonus"
---

<objective>
Wire sell commands (single, bulk, junk) into the narrative intent system with correct perk bonuses, and complete event feed color coding.

Purpose: Players can sell items naturally through the narrative console with full perk bonus support, and see color-coded event messages matching their category (combat=red, reward=gold, system=gray, social=blue).

Output: Updated intent.ts with three sell paths, fixed sell_all_junk reducer, complete KIND_COLORS, and tests.
</objective>

<execution_context>
@C:/Users/Dell/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Dell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/034-narrative-ui-integration/034-RESEARCH.md

@spacetimedb/src/reducers/intent.ts
@spacetimedb/src/reducers/items.ts
@spacetimedb/src/helpers/economy.ts
@src/components/NarrativeMessage.vue
@spacetimedb/src/reducers/intent.test.ts
</context>

<interfaces>
<!-- Key imports and helpers the executor needs -->

From spacetimedb/src/helpers/economy.ts:
```typescript
export function computeSellValue(baseValue: bigint, vendorSellMod: bigint): bigint;
```

From spacetimedb/src/helpers/perks.ts (or wherever getPerkBonusByField lives):
```typescript
export function getPerkBonusByField(ctx: any, characterId: bigint, field: string, level: bigint): number;
```

From spacetimedb/src/helpers/narrative.ts:
```typescript
export function appendPrivateEvent(ctx: any, characterId: bigint, ownerUserId: any, kind: string, message: string): void;
```

Existing sell pattern in intent.ts (lines ~887-948) uses computeSellValue but NOT getPerkBonusByField.
Existing sell_all_junk reducer in items.ts (lines ~219-242) uses NEITHER getPerkBonusByField NOR computeSellValue.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix sell perk bonus + add sell all junk and sell N commands</name>
  <files>spacetimedb/src/reducers/intent.ts, spacetimedb/src/reducers/items.ts, spacetimedb/src/reducers/intent.test.ts</files>
  <behavior>
    - Test: sell single item applies getPerkBonusByField vendorSellBonus AND computeSellValue (not just computeSellValue)
    - Test: sell all junk command finds all non-equipped junk items, applies perk bonus per item, returns gold total and summary message
    - Test: sell all junk with no junk returns "no junk to sell" system message
    - Test: sell 3 <item> sells min(3, available) matching items with perk bonus applied to each
    - Test: sell commands require vendor NPC at location
  </behavior>
  <action>
1. **Fix existing sell in intent.ts (~line 887-948):** Add `getPerkBonusByField(ctx, character.id, 'vendorSellBonus', character.level)` call before `computeSellValue`. Apply perk multiplier: if vendorSellBonus > 0 and baseValue > 0n, multiply baseValue by (100 + vendorSellBonus) / 100 before calling computeSellValue.

2. **Add `sell all junk` / `sell junk` branch in intent.ts BEFORE the existing `sell <item>` block** (so it matches first):
   - Check for vendor NPC at location (same pattern as existing sell)
   - Iterate `ctx.db.item_instance.by_owner.filter(character.id)`, skip equipped items
   - For each: look up template, check `isJunk` flag
   - Apply perk bonus + computeSellValue per item
   - Delete item affixes, delete item instance
   - Sum total gold, collect names (show first 3 + "and N more")
   - Emit reward event with summary or system event "no junk to sell"

3. **Add `sell N <item>` branch** (regex: `/^sell\s+(\d+)\s+(.+)$/i`):
   - Parse quantity and item name
   - Find matching items by name (case-insensitive includes, same pattern as existing sell)
   - Sell up to N matching non-equipped items, applying perk bonus + computeSellValue each
   - Summary event with count and total gold

4. **Fix sell_all_junk reducer in items.ts:** Add getPerkBonusByField and computeSellValue calls (same fix as intent.ts). This ensures both the reducer path and intent path produce consistent results.

5. **Write tests in intent.test.ts:** Use the existing mock DB pattern from that file. Test sell single (with perk), sell all junk (with/without junk), sell N, and vendor-required check.

Import `getPerkBonusByField` from helpers if not already imported in intent.ts. Import `computeSellValue` from helpers/economy.ts if not already imported in items.ts sell_all_junk.
  </action>
  <verify>
    <automated>cd spacetimedb && npx vitest run src/reducers/intent.test.ts -t "sell" --reporter=verbose</automated>
  </verify>
  <done>All sell commands work with perk bonuses: single sell applies getPerkBonusByField, sell all junk iterates and summarizes, sell N sells correct quantity. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete event feed color coding with tests</name>
  <files>src/components/NarrativeMessage.vue, src/components/NarrativeMessage.test.ts</files>
  <behavior>
    - Test: KIND_COLORS contains key 'social' with value '#74c0fc'
    - Test: KIND_COLORS contains key 'ability' with value '#74c0fc'
    - Test: KIND_COLORS contains key 'damage' (combat/red category)
    - Test: KIND_COLORS contains key 'reward' with value '#ffd43b'
    - Test: KIND_COLORS contains key 'system' with value '#adb5bd'
  </behavior>
  <action>
1. **Write tests first** in `src/components/NarrativeMessage.test.ts`:
   - Import KIND_COLORS from NarrativeMessage.vue (you may need to export it -- add `export` to the KIND_COLORS const if not already exported).
   - Assert required keys exist: `social`, `ability`, `damage`, `reward`, `system`.
   - Assert specific hex values: `social` = `#74c0fc`, `ability` = `#74c0fc`, `reward` = `#ffd43b`, `system` = `#adb5bd`.

2. **Update KIND_COLORS map in NarrativeMessage.vue** (line 60):
   - Add missing entries:
     - `social: '#74c0fc'` (blue -- for social-typed events)
     - `ability: '#74c0fc'` (blue -- used by use_ability event emission)
   - Verify existing colors match requirements:
     - combat = red: `damage: '#ff6b6b'`, `blocked: '#ff6b6b'` -- CORRECT
     - reward = gold: `reward: '#ffd43b'` -- CORRECT
     - system = gray: `system: '#adb5bd'` -- CORRECT
   - Ensure the default fallback `#ced4da` (line 90) is reasonable for any unmatched kinds.

3. Ensure the `export` keyword is added to KIND_COLORS so it can be imported in the test file.

This is a small change -- just adding 1-2 entries to the existing map + exporting it. Do NOT restructure or rename existing color entries.
  </action>
  <verify>
    <automated>cd C:/projects/uwr && npx vitest run src/components/NarrativeMessage.test.ts --reporter=verbose</automated>
  </verify>
  <done>KIND_COLORS map includes social and ability entries with correct hex values. All required color categories (combat=red, reward=gold, system=gray, social=blue) verified by automated tests.</done>
</task>

</tasks>

<verification>
- `cd spacetimedb && npx vitest run --reporter=verbose` -- all tests pass
- `cd C:/projects/uwr && npx vitest run src/components/NarrativeMessage.test.ts --reporter=verbose` -- KIND_COLORS tests pass
- Grep intent.ts for `getPerkBonusByField` -- appears in sell section
- Grep NarrativeMessage.vue for `social` -- appears in KIND_COLORS
</verification>

<success_criteria>
- sell <item> applies vendor sell perk bonus correctly
- sell all junk sells all junk items, applies perks, shows summary
- sell N <item> sells correct quantity with perks
- KIND_COLORS has social and ability entries with correct hex values, verified by automated tests
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/034-narrative-ui-integration/034-01-SUMMARY.md`
</output>
