// Ambient stub for generated Spacetime client bindings.
// These declarations are intentionally permissive to avoid blocking the workspace typecheck
// during local development. Regenerate proper bindings with `pnpm generate:bindings` and
// remove this stub when the generated files are ready and their typings align with the SDK.

declare module 'services/orchestrator/src/module_bindings' {
    export const DbConnection: any;
    export const RemoteTables: any;
    export const RemoteReducers: any;
    export type EventContext = any;
    export type Reducer = any;
    export default any;
}

export { };
